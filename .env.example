# These are auto-populated when you connect Vercel KV (Upstash Redis) to your project.
# You do NOT need to fill these in manually for production.
# See README.md for setup steps.

KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
